---
const { titulo, descripcion, imagen } = Astro.props;
---

<div class="p-4 bg-gray-100 rounded-lg shadow-md max-w-xs text-center">
  <img src={imagen} alt={titulo} class="w-full h-32 object-cover rounded-md mb-4">
  <h3 class="text-xl font-medium">{titulo}</h3>
  <p>{descripcion}</p>
</div>
